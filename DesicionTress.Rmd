---
title: "Music_PredictionEDA"
author: "Pavani Samala"
date: "`r Sys.Date()`"
output:
  html_document:
    code_folding: hide
    number_sections: false
    toc: yes
    toc_depth: 3
    toc_float: yes
  pdf_document:
    toc: yes
    toc_depth: '3'
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(warning = F, results = "hide", message = F)
library(ezids)
loadPkg("tree")
```


Reading in the dataset: All come in as expected (date is a character, but we don't need this because it's not really realted to the song data)

```{r}
setwd("/Users/pavan/Desktop/Git/6101 project/DS-6101-Final")
music = data.frame(read.csv("music_genre.csv"))
str(music)
```

```{r}
# drop those blank genre values 
music = music[!(music$music_genre == ""),]
new_music<-subset(music, music_genre=='Electronic' | music_genre=='Jazz')
new_music$music_genre = factor(new_music$music_genre)
str(new_music)
```



Prepossessing the Genre Column with dummy variables
```{r}

#genres<-c("Electronic","Anime","Jazz","Alternative","Country","Rap","Blues","Rock","Classical","Hip-Hop")
new_genres<-c()
for (i in music$music_genre) 
  {
  if (i=="Electronic")
    new_genres<-append(new_genres,values=1)
  else
    new_genres<-append(new_genres,values=0)
}

print(new_genres)
```


```{r, results = 'markup'}
loadPkg("rpart")

set.seed(10)
treefit <- rpart(music_genre ~ liveness + loudness + mode, data=new_music, method="class", control = list(maxdepth = 15) )
summary(treefit)
loadPkg("rattle")
fancyRpartPlot(treefit)


```



```